## ขอบเขตการพัฒนาระบบ (System Scope & Core Features)

### 1 ระบบยืนยันตัวตนและการกำหนดสิทธิ์ (Authentication & RBAC)
* เข้าสู่ระบบด้วยความปลอดภัยรูปแบบ JSON Web Token (JWT)
* แบ่งสิทธิ์การใช้งานออกเป็น 2 ระดับ:
  * **User (พนักงานทั่วไป):** ยื่นคำขอเบิกอุปกรณ์, ตรวจสอบสถานะคำขอ, ดูประวัติการเบิก-คืนของตนเอง, สแกน QR Code เพื่อยืนยันการรับ/คืน
  * **Admin (เจ้าหน้าที่ IT):** จัดการข้อมูลอุปกรณ์ในคลัง, อนุมัติหรือปฏิเสธคำขอเบิก (1 ขั้นตอน), ตรวจสอบประวัติการเคลื่อนไหว และดูรายงานสถิติ

### 2 ระบบจัดการคลังอุปกรณ์แบบไฮบริด (Hybrid Inventory Management)
บริหารจัดการอุปกรณ์ทั้งหมดผ่านอินเทอร์เฟซเดียว โดยแยกพฤติกรรมผ่านฟิลด์สถานะ `is_consumable`:
* **สินทรัพย์ถาวร (`is_consumable = false`):** 
  * บังคับระบุหมายเลขซีเรียล (Serial Number)
  * มีวงจรชีวิตการใช้งาน (ยืม -> กำลังใช้งาน -> ส่งคืน)
* **ของสิ้นเปลือง (`is_consumable = true`):** 
  * บริหารจัดการแบบตัดยอดจำนวนคงเหลือ (Stock Quantity)
  * เมื่อคำขอได้รับอนุมัติ ระบบจะตัดสต็อกทันทีโดยไม่มีสถานะการส่งคืน


---

### การเพิ่มประสิทธิภาพการจัดการข้อมูลขนาดใหญ่ (Performance Optimization)
* **Frontend Performance:** ใช้ AG-Grid บน Angular สำหรับแสดงผลตารางประวัติการเบิก-จ่ายที่มีรายการจำนวนมาก เพื่อให้เรนเดอร์ข้อมูลได้อย่างรวดเร็ว
* **Database Optimization:** ออกแบบการสรุปผลสถิติบน Dashboard (ApexCharts) โดยใช้เทคนิค SQL Aggregation จากฝั่ง PostgreSQL เพื่อคำนวณผลรวมและจัดกลุ่มข้อมูลจากฐานข้อมูลโดยตรง ลดการดึงข้อมูลดิบขนาดใหญ่มาประมวลผลบนเบราว์เซอร์

---

## เครื่องมือและสถาปัตยกรรมทางเทคนิค (Tech Stack)

| ส่วนประกอบ (Component) | เทคโนโลยี / เครื่องมือที่ใช้ |
| :--- | :--- |
| **Frontend Framework** | Angular (TypeScript, HTML, CSS) |
| **Data Grid & Visualization** | AG-Grid, ApexCharts |
| **Backend API** | Node.js |
| **Database** | PostgreSQL |
